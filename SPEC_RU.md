# Спецификация поведения JSONSpecs

**Версия:** 1.0.0-rc.4 · **Статус:** Release Candidate — тег `v1.0.0` вешается после
кросс-реализационного сравнения на живом стенде (см. README репозитория, раздел
о релизном процессе). English version: [SPEC.md](SPEC.md) — при расхождениях
нормативен английский текст.

Эта спецификация — канон поведения рантайма для данной версии. Она описывает только
ожидаемое поведение. Аргументация каждого решения живёт в отдельном документе —
`DECISIONS_RU.md` / `DECISIONS.md`, — который, в свою очередь, ссылается на
прикладной прототип (`source/`), на опыте которого эти решения приняты.

**Нотация.** `[D1]`…`[D25]` — ссылки на нумерованные решения в `DECISIONS_RU.md`;
`[DR-I]`…`[DR-VI]` — ссылки на аддендумы реестра.
MUST / MUST NOT / SHOULD / MAY — по RFC 2119 (в переводе: ОБЯЗАН / НЕ ДОЛЖЕН /
СЛЕДУЕТ / МОЖЕТ; ниже используются английские ключевые слова).

---

## 1. Введение и скоуп

Спецификация определяет наблюдаемое поведение рантайма правил JSONSpecs:

1. формат артефактов правил и снэпшотов;
2. семантику встроенных операторов — что они решают, а не как;
3. формат и содержание результата исполнения.

Две конформные реализации, получив один и тот же снэпшот, один `payload` и один
`context`, MUST выдать идентичные нормативные результаты (нормативная поверхность
определена в разделе 7) и MUST принимать/отвергать одно и то же множество входов.
Для снэпшотов с не-встроенными операторами гарантия ограничена §7.1. `[D17]` Канон —
текст спецификации; набор фикстур — обязательная, но не достаточная проверка
декларации конформанса (§7.3).

Спецификация не определяет API, имена функций, внутренние представления, стратегию
компиляции, момент валидации, характеристики производительности и транспортные
лимиты на размер payload или результата. Реализация MAY компилировать снэпшоты
заранее, интерпретировать напрямую, строить любые промежуточные структуры или не
строить никаких — эти выборы невидимы для конформанса. `[D12]`

Сквозной принцип проектирования: **DSL никогда не подразумевает решений за
аналитика.** Обязательно ли поле, обязан ли присутствовать контекст, какой код и
текст даёт ошибка отсутствия — каждое из этого есть явное правило, написанное
аналитиком, и никогда — побочный эффект value-оператора или удобство рантайма
(см. D13, D14).

Кастомные операторы — вне спецификации. Спецификация определяет только интерфейс
расширения (форму результата оператора и правило отвержения неизвестного оператора —
раздел 4); поведение любого не-встроенного оператора — обещание конкретной
реализации, а не спецификации. `[D10]`

---

## 2. Модель данных

### 2.1 Значения

*Значение* — это JSON-значение: `null`, boolean, число, строка, массив или объект.
`undefined` в модели данных не существует. Там, где хост-язык различает
«отсутствует» и «null», нормативно только JSON-видимое различие: ключа нет — против
ключа со значением `null`.

Все входы (артефакты, снэпшот, payload, context) и нормативный результат —
JSON-документы. Структуры, не проходящие через JSON-сериализацию (циклические
ссылки, хост-специфичные типы), — вне модели. Числовые токены JSON входят в модель
только после нормативного преобразования §2.2; переполнение до бесконечности
отвергается на соответствующей границе (§4.10, §5.1).

**Максимальная глубина.** Глубина определена алгоритмически: скаляр и пустой
контейнер имеют глубину 1; непустой контейнер — 1 + максимум глубин его членов.
Каждый входной документ — каждый артефакт, payload, context — MUST NOT превышать
глубину **256**: глубина 256 принимается, 257 отвергается (раздел 5). `[D9][DR-IV]`
Лимит — *входной гард*, а не ограничение результата: результат, построенный из
ограниченных входов, ограничен по построению плюс фиксированная обёртка конверта, и
нормативного лимита глубины на него нет: перенос принятого `issue.meta` в конверт
результата не должен внезапно делать результат невалидным.

Значения, не являющиеся JSON-документами (циклы хост-языка, функции, BigInt и
подобное), — целиком вне модели данных; как граница API реализации обращается с
ними — забота адаптера, вне скоупа. `[DR-IV]`

**Зарезервированные ключи.** Ключи объектов `__proto__`, `prototype` и
`constructor` запрещены в payload и context на любой глубине. Это правило контракта,
а не платформенная защита: реализации на платформах без прототипной семантики MUST
отвергать эти ключи точно так же — иначе одинаковые входы давали бы разные вердикты
на разных реализациях. `[D9]`

### 2.2 Числа

Область чисел — ровно конечные значения **IEEE 754 binary64**. `[D1][D23]`

- Каждый числовой токен JSON MUST быть преобразован в binary64 с округлением
  round-to-nearest, ties-to-even. Это преобразование нормативно и одинаково для
  снэпшота, payload, context и параметров пользовательских операторов.
- Если преобразование даёт `+Infinity` или `-Infinity`, документ не входит в
  модель: снэпшот отвергается, а payload/context дают структурированный `ABORT`
  (§5.1). Реализация с произвольной точностью, например Java `BigDecimal`, MUST
  выполнить это преобразование явно до исполнения.
- Любой конечный результат преобразования принимается. Поэтому `0.1` принимается
  как ближайшее binary64-значение, а токен `9007199254740993` — как
  `9007199254740992`. Формулировки «вне гарантии детерминизма» нет.
- Переполнение отвергается; underflow и subnormal-значения обрабатываются обычным
  round-to-nearest-even. `-0` нормализуется в `+0` на всех нормативных поверхностях.
- Целые в диапазоне ±(2^53 − 1) представляются точно. `1` и `1.0` — одно значение.
  `is_integer` принимает любое конечное binary64-число с нулевой дробной частью:
  `1.0` проходит, `1.5` — нет.

**Числовые строки.** В упорядоченных сравнениях (§2.5) строка *числовая* тогда и
только тогда, когда соответствует грамматике:

```
^[+-]?[0-9]+(\.[0-9]+)?([eE][+-]?[0-9]+)?$
```

Строка, совпавшая с грамматикой, является числовой только если преобразование
round-to-nearest-even даёт конечный binary64. Поэтому `"1e400"` — допустимая
JSON-строка, но не числовая строка для упорядоченного сравнения. Строки вне
грамматики также числами не являются, что бы ни принимал снисходительный парсер
хост-языка (`" 5"`, `"0x10"`, `"5,0"`, `"Infinity"` — не числа).

### 2.3 Строки

Строки — последовательности кодовых точек Unicode.

**Длина.** Везде, где спецификация измеряет длину строки (`length_equals`,
`length_max`), единица — **кодовая точка Unicode**. `[D2]` Суррогатная пара
считается за одну. Реализации, у которых нативная длина строки — UTF-16 code units
(JavaScript, Java), MUST считать кодовые точки явно.

Графемные кластеры не используются никогда: их определение зависит от версии
Unicode и сделало бы результаты зависимыми от релиза.

### 2.4 Равенство

Два значения *равны*, когда:

- оба `null`; или
- оба booleans с одинаковым значением; или
- оба числа с одним binary64-значением (`1 == 1.0`); или
- обе строки с одинаковой последовательностью кодовых точек.

Значения разных типов не равны никогда: `1` ≠ `"1"`, `false` ≠ `0`, `null` ≠ `""`.
Коэрции нет. Массивы и объекты в этом отношении не равны ничему (равенство
определено для скалярного сравнения в операторах; глубокого структурного равенства
в операторной модели нет).

### 2.5 Упорядоченное сравнение

Упорядоченное сравнение (`greater_than`, `less_than`, `field_*_field`) работает над
двумя операндами, классифицируемыми независимо:

| Операнд | Классификация |
| --- | --- |
| JSON-число | **число** |
| строка по числовой грамматике (§2.2) | **число** (конвертируется в binary64) |
| строка по `^[0-9]{4}-[0-9]{2}-[0-9]{2}$`, являющаяся валидной датой пролептического григорианского календаря | **дата** |
| всё остальное (booleans, `null`, массивы, объекты, прочие строки) | **неклассифицируемо** |

Календарная валидность включает диапазон месяцев 01–12, валидный для месяца
диапазон дней и правила високосных лет для февраля; `2026-02-30` и `2026-13-01` —
не даты. Диапазон лет — 0000–9999. Порядок дат — хронологический (эквивалентно:
лексикографический над канонической строкой `YYYY-MM-DD`).

Сравнение *определено* только когда оба операнда классифицируются в **один** вид
(оба числа или обе даты). Если любой операнд неклассифицируем или виды различаются,
сравнение *неопределено*, и оператор даёт `FAIL` для присутствующих операндов.

Классификация дизъюнктна по построению: никакая строка не является одновременно
числовой и датой.

### 2.6 Пустота

Поле *пусто*, когда оно:

- **отсутствует** — его путь не резолвится в плоском payload (§2.7); или
- `null`; или
- `""` (пустая строка).

`false`, `0`, `[]` и `{}` — **не** пусты.

### 2.7 Пути, флаттенинг и резолюция полей

**Синтаксис пути.** Ссылка на поле — dot-notation-путь по плоской карте payload:

```
order.amount
accounts[0].balance
accounts[*].balance          ← wildcard, §3.6
$context.currentDate         ← доступ к контексту
```

**Нормативная форма входа.** `[D15]` Payload и context — обычные вложенные
JSON-объекты. Плоская карта путь → значение ниже — **внутренняя проекция**,
определяющая резолюцию путей, а не формат входа: приём уже плоского входа — адаптер
реализации вне контракта. Чтобы каждый лист имел ровно один однозначный путь, ключи
объектов payload и context MUST NOT быть пустыми и MUST NOT содержать `.`, `[` или
`]` — такие ключи неадресуемы, вход отвергается (§5.1,
`INVALID_PAYLOAD_KEY` / `INVALID_CONTEXT_KEY`).

**Флаттенинг (внутренняя проекция).** Определён нормативно:

- Ключ `k` вложенного объекта под префиксом `p` порождает пути `p.k…`; элемент
  массива с индексом `i` под префиксом `p` — пути `p[i]…`.
- **Листья** — это: скаляры (`null`, boolean, число, строка), пустой объект `{}` и
  пустой массив `[]`. Путь листа отображается в его значение.
- **Непустой контейнер сам в плоской карте не присутствует** — только пути его
  потомков. Для `{"a": [1]}` плоская карта — `{"a[0]": 1}`; путь `a` *отсутствует*.
  Для `{"a": []}` плоская карта — `{"a": []}`, и `a` резолвится в значение `[]`
  (непустое по §2.6).

**Резолюция.** Резолюция пути `f` по плоской карте даёт либо
*(присутствует, значение)*, либо *отсутствует*. Частичной резолюции, фолбэков и
значений по умолчанию нет.

**Грамматика путей.** `[DR-IV]` Ссылка на поле MUST соответствовать:

```ebnf
path      = [ "$context." ] , segment , { "." , segment } ;
segment   = key , { index } ;
key       = key-char , { key-char } ;                  (* непустой *)
key-char  = ? любая кодовая точка, кроме "." "[" "]" ? ;
index     = "[" , ( "0" | nz-digit , { digit } ) , "]"  (* без ведущих нулей *)
          | "[*]" ;
```

Пути с нарушением грамматики (пустые сегменты, `a..b`, индексы с ведущим нулём вроде
`a[01]`) делают артефакт невалидным. `[*]` допустим только там, где это разрешает
§3.6; пути `value_field` и `$context.*` MUST NOT содержать `[*]`.

**Доступ к контексту.** Пути с зарезервированным префиксом `$context.` резолвятся
по входу `context` вместо payload, с той же семантикой резолюции. Wildcards внутри
путей `$context.*` не допускаются (отвержение артефакта). Префикс зарезервирован:
ключ payload, буквально названный `$context`, не имеет специального смысла и
недостижим через пути `$context.*`.

Legacy-механизма передачи контекста внутри payload под ключом `__context` в этой
спецификации **не существует**: `__context` — обычный ключ payload без специальной
семантики. `[D11]`

---

## 3. Семантика операторов

### 3.1 Модель оператора

*Правило* применяет один оператор к payload и, возможно, context. Правило не имеет
роли `check` или `predicate`: это одно бизнес-условие, а последствия его результата
определяются местом использования (§5.3–§5.4). `[D19]`

Нормативный исход оператора — ровно одно из `PASS`, `FAIL`, `SKIP`:

- `PASS` — условие выполнено;
- `FAIL` — условие не выполнено;
- `SKIP` — условие семантически неприменимо к этому входу.

Возвращённое значение вне этого закрытого enum, включая строку `EXCEPTION`, даёт
`ABORT OPERATOR_CONTRACT_VIOLATION`. Брошенное исключение или panic реализации
даёт `ABORT OPERATOR_FAULT`. Реакция не зависит от того, вызвано правило шагом или
из `when`. Бизнес-остановка выражается только `FAIL` правила с
`issue.level: "EXCEPTION"`; оператор не выбирает уровень и не создаёт issue. `[D17][D19]`

Поведение оператора — чистая функция от разрезолвленных значений полей, параметров
правила (`value`, `value_field`, `fields`, `dictionary`, `flags`, пользовательского
`params`), содержимого
словаря и context. Операторы MUST NOT зависеть от места вызова, порядка исполнения,
часов, локали или иного ambient-состояния.

**Поведение при отсутствии поля.** `[D13][D19]`

- **Presence-семантика** (`not_empty`, `is_empty`, `not_true`, `any_filled`):
  отсутствие входит в предметную область; исход указан в таблице ниже.
- **Value-семантика** (все остальные): если обязательный операнд отсутствует,
  оператор не вызывается, а правило получает `SKIP`. Для `field_*_field` это
  относится к обоим операндам.

`SKIP` в rule-шаге ничего не порождает; в `when` он интерпретируется как `false`.
Обязательность поля поэтому всегда выражается отдельным presence-правилом. Реализация
SHOULD отражать `SKIP` в trace, но trace не меняет нормативный результат.

**Строго строковые операторы** `[D3][DR-I]`: `contains`, `matches_regex` и
`not_matches_regex` требуют строку; присутствующее нестроковое значение даёт `FAIL`.
Хостовая стрингификация не применяется.

### 3.2 Встроенные операторы

Все операторы этой таблицы разрешены как в rule-шаге, так и в `when`. Колонка
«Отсутствие» задаёт исход правила, когда путь не резолвится. Все сравнения используют
§2.4 и §2.5 без иной коэрции.

| Оператор | Параметры | PASS когда | Отсутствие |
| --- | --- | --- | --- |
| `not_empty` | — | поле не пусто (§2.6) | FAIL |
| `is_empty` | — | поле пусто (§2.6) | PASS |
| `not_true` | — | значение — что угодно, кроме boolean `true` | PASS |
| `any_filled` | `fields: path[]` | хотя бы одно из перечисленных полей не пусто (§2.6); `field` не используется | отсутствие = пусто; FAIL, когда все перечисленные поля пусты |
| `is_boolean` | — | значение — boolean | SKIP |
| `is_string` | — | значение — строка | SKIP |
| `is_number` | — | значение — число | SKIP |
| `is_integer` | — | значение — число с нулевой дробной частью | SKIP |
| `equals` | `value` | значение равно `value` (§2.4) | SKIP |
| `not_equals` | `value` | значение не равно `value` (§2.4) | SKIP |
| `contains` | `value: string` | значение — строка, содержащая `value` как подстроку | SKIP |
| `matches_regex` | `value: pattern`, `flags?` | строка содержит совпадение паттерна (§3.4) | SKIP |
| `not_matches_regex` | `value: pattern`, `flags?` | строка не содержит совпадений паттерна | SKIP |
| `greater_than` | `value: число \| дата-строка` | сравнение определено (§2.5) и поле > value | SKIP |
| `less_than` | `value: число \| дата-строка` | сравнение определено и поле < value | SKIP |
| `length_equals` | `value: число` | строка состоит ровно из `value` кодовых точек; нестрока → FAIL | SKIP |
| `length_max` | `value: число` | строка состоит не более чем из `value` кодовых точек; нестрока → FAIL | SKIP |
| `field_equals_field` | `value_field: path` | оба поля присутствуют и равны (§2.4) | SKIP, если любое отсутствует |
| `field_not_equals_field` | `value_field: path` | оба присутствуют и не равны | SKIP, если любое отсутствует |
| `field_greater_than_field` | `value_field: path` | оба присутствуют, сравнение определено, field > value_field | SKIP, если любое отсутствует |
| `field_less_than_field` | `value_field: path` | оба присутствуют, сравнение определено, field < value_field | SKIP, если любое отсутствует |
| `field_greater_or_equal_than_field` | `value_field: path` | оба присутствуют, сравнение определено, field ≥ value_field | SKIP, если любое отсутствует |
| `field_less_or_equal_than_field` | `value_field: path` | оба присутствуют, сравнение определено, field ≤ value_field | SKIP, если любое отсутствует |
| `in_dictionary` | `dictionary: {type:"static", id}` | значение матчит запись словаря (§3.5) | SKIP |
| `not_in_dictionary` | `dictionary: {type:"static", id}` | значение не матчит ни одну запись словаря (§3.5) | SKIP |

`any_filled` принимает только `fields[]`; legacy-алиаса `paths[]` нет. Для
`greater_than`/`less_than` значение `value` классифицируется по тем же правилам,
что и payload. Идиома «обязательно и должно удовлетворять X» остаётся парой правил:
presence-проверка и value-проверка. `[D11][D13]`

### 3.3 Интерпретация исхода

| Исход | Rule-шаг | Лист `when` |
| --- | --- | --- |
| `PASS` | ничего не порождает | `true` |
| `FAIL` | создаёт issue из `rule.issue` | `false`, issue не создаётся |
| `SKIP` | ничего не порождает | `false` |

Объект `issue` не участвует в логическом вычислении. Поэтому правило с
`issue.level: "EXCEPTION"` MAY использоваться в `when` и остаётся там обычным
условием без побочных эффектов. `[D19]`

### 3.4 Регулярные выражения

`[D4]` Раздел определяет язык паттернов полностью. Это переносимое подмножество,
выбранное так, чтобы каждая мейнстримная бэкенд-платформа исполняла его штатным
regex-движком (строгое подмножество RE2, ECMAScript-с-`u`, `java.util.regex` и
non-backtracking-режима .NET). Язык реализуем автоматным движком за
линейное время (он — подмножество RE2); это свойство *языка*, а не любого
исполнения: бэктрекинговые движки могут исполнять отдельные паттерны подмножества
(например, `(a+)+$`) суперлинейно. `[D16]` *Security-нота (информативно):*
реализациям SHOULD исполнять паттерны автоматным движком либо применять
эквивалентные митигации; выбор — вне скоупа контракта `[D12]`.

#### 3.4.1 Препроцессинг

Перед разбором к строке паттерна применяется ровно один проход замен: каждая пара
последовательных обратных слэшей `\\` заменяется одним `\`. Проход не итеративен.
`[D4.3]` (Следствие: на уровне JSON-исходника и `"^\\d+$"`, и `"^\\\\d+$"`
обозначают паттерн `^\d+$`.)

#### 3.4.2 Грамматика

После препроцессинга паттерн MUST соответствовать:

```ebnf
pattern      = alternation ;
alternation  = concat , { "|" , concat } ;
concat       = { element } ;
element      = anchor | quantified ;
anchor       = "^" | "$" ;
quantified   = atom , [ quantifier ] ;
quantifier   = ( "*" | "+" | "?" | counted ) ;            (* без lazy/possessive *)
counted      = "{" , int , [ "," , [ int ] ] , "}" ;      (* {n} {n,} {n,m}, n ≤ m *)
atom         = literal | "." | escape | class | group ;
group        = "(" , [ "?:" ] , alternation , ")" ;
class        = "[" , [ "^" ] , class-item , { class-item } , "]" ;
class-item   = class-atom , [ "-" , class-atom ]          (* диапазон: левый ≤ правый *)
             | class-escape ;
class-atom   = class-literal | escaped-meta | char-escape ;
escape       = escaped-meta | char-escape | class-escape ;
escaped-meta = "\" , ( "\" | "." | "*" | "+" | "?" | "(" | ")" | "[" | "]"
                     | "{" | "}" | "|" | "^" | "$" | "/" | "-" ) ;
char-escape  = "\n" | "\r" | "\t" ;
class-escape = "\d" | "\D" | "\w" | "\W" | "\s" | "\S" ;
int          = digit , { digit } ;                        (* значение ≤ 1000 *)
```

`literal` — любая кодовая точка, кроме метасимволов `\ . * + ? ( ) [ ] { } | ^ $`.
`class-literal` — любая кодовая точка, кроме `\ ] -` (литеральный `-` пишется
первым, последним или экранированным; литеральный `^` внутри класса — где угодно,
кроме первой позиции).

**Явно исключены** (их присутствие делает паттерн — и, следовательно, снэпшот —
невалидным): backreferences, lookahead/lookbehind `(?= (?! (?<= (?<!`, именованные
группы `(?<name>`, границы слова `\b \B`, инлайн-флаги `(?i)`, hex/unicode-эскейпы
`\xHH \uHHHH \x{…}` (символы Unicode пишутся литерально или экранируются на уровне
JSON-строки, которая уже даёт `\uXXXX`), POSIX-классы `[:alpha:]`, вложенные
классы, восьмеричные эскейпы и любой эскейп, не перечисленный в грамматике.

**Нормативные лимиты** (защита от расходящихся внутренних лимитов движков): каждый
`int` в квантификаторе MUST быть ≤ 1000; препроцессированный паттерн MUST быть
≤ 1024 кодовых точек. Паттерны, нарушающие грамматику или лимиты, делают артефакт
невалидным — это отвержение артефакта, а не рантайм-ошибка.

#### 3.4.3 Семантика матчинга

- Субъект — последовательность кодовых точек Unicode; `.` матчит любую одну
  кодовую точку, кроме U+000A LINE FEED. (JavaScript-реализации MUST компилировать
  с флагом `u` для code-point-семантики.)
- `\d` = `[0-9]`, `\w` = `[0-9A-Za-z_]`, `\s` = `[ \t\n\r\f\v]`, верхний регистр —
  дополнения. **ASCII-семантика независимо от дефолтов платформы** — движки с
  Unicode-осведомлёнными дефолтами (например, Rust `regex`) MUST компенсировать.
  `[D4.1]`
- Матчинг — **поиск**: паттерн совпадает, если совпадает любая подстрока (включая
  пустую). Авторы якорят `^`/`$` явно.
- Флаги: только `i`, `m`, `s`, каждый не более раза, в любом порядке; любая иная
  строка флагов делает артефакт невалидным.
  - `i` — регистронезависимость через **Unicode simple case folding**,
    culture-invariant, с пиннингом на **Unicode 16.0.0**: нормативный маппинг —
    <https://www.unicode.org/Public/16.0.0/ucd/CaseFolding.txt>, только статусы `C`
    и `S` (записи `F` и `T` не используются). Эквивалентность симметрична и
    транзитивна — две кодовые точки эквивалентны ⟺ их simple-фолдинги равны,
    никогда не односторонняя замена. Следствия: `и` ≡ `И`; `K` (U+212A) ≡ `k`;
    `ſ` ≡ `s`; `Σ` ≡ `σ` ≡ `ς`; `ß` ≡ `ẞ` (U+1E9E), но `ß` ≢ `SS` (full-фолдинг
    исключён); турецкие `İ`/`ı` фолдятся сами в себя и не матчат ASCII `i`/`I`;
    поведение турецкой локали MUST NOT протекать. Реализации, чей движок несёт иную
    версию Unicode, MUST компенсировать. `[D4.2][D18]`
  - `m` — `^`/`$` дополнительно матчат после/перед U+000A.
  - `s` — `.` матчит и U+000A.

### 3.5 Словари

Словарь — именованный список `entries`. Значение payload *матчит* запись, когда:

| Форма записи | Матчит когда |
| --- | --- |
| скаляр (строка, число, boolean; записи `null` делают артефакт невалидным) | значение равно записи (§2.4) |
| объект с полями `code` и/или `value` | значение равно `entry.code` **или** равно `entry.value` (участвуют все присутствующие поля) |
| объект без обоих полей | не матчит никогда (артефакт SHOULD отвергаться схемой; при наличии — не-матч) |

Матчинг — равенство §2.4: строгое, без коэрции; тип значения payload должен
совпадать с типом записи. `in_dictionary` даёт `PASS`, когда матчит любая запись;
`not_in_dictionary` даёт `PASS`, когда не матчит ни одна. `dictionary.type` MUST
быть `"static"`.

### 3.6 Wildcards и агрегация

#### 3.6.1 Резолюция wildcard и порядок перечисления `[D5]`

Путь `field` может содержать сегменты `[*]`. Сегмент `[*]` матчит ровно сегменты —
неотрицательные целые индексы в этой позиции плоской внутренней карты. Резолюция
даёт список конкретных путей, присутствующих во входном вложенном JSON после
флаттенинга.

Порядок перечисления нормативен:

1. индексы упорядочиваются как числа по возрастанию; дыры допустимы;
2. несколько `[*]` упорядочиваются лексикографически по кортежу индексов, левый
   сегмент старше («одометр»).

Этот порядок определяет порядок поэлементных issues и tie-break `MIN`/`MAX`.

#### 3.6.2 Агрегация `[D20]`

Правило, чьё `field` содержит `[*]`, MUST иметь `aggregate` с явным `mode`.
`aggregate` на поле без wildcard невалиден. `value_field` MUST NOT содержать
wildcard; индексное сопоставление двух wildcard-путей в версии 1 не определено.

Поля `aggregate`:

| Поле | Ограничение |
| --- | --- |
| `mode` | обязательное: `ALL`, `ANY`, `COUNT`, `MIN` или `MAX` |
| `onEmpty` | `PASS`, `FAIL` или `SKIP`; дефолт `SKIP` |
| `issueMode` | `EACH` или `SUMMARY`; только для `ALL`/`ANY`; обязательно, если у правила есть `issue`, и запрещено, если `issue` отсутствует |
| `op`, `value` | только `COUNT`; `op` ∈ `== != > >= < <=`, дефолт `>=`; целое неотрицательное `value` обязательно |

Для `COUNT`, `MIN` и `MAX` `issueMode` запрещён: их провал всегда даёт один
summary-issue. Legacy-режим `EACH` как значение `mode` не существует.

**Популяция и `SKIP` для `ALL`/`ANY`/`COUNT`.** Исполнение этих трёх режимов идёт
в следующем порядке:

1. Wildcard резолвится в структурный список совпадений.
2. Если список пуст, исход берётся из `onEmpty`.
3. Для каждого совпадения вычисляется оператор. `SKIP` исключается из эффективной
   популяции; `matched` — размер структурного списка, `evaluated` — число
   `PASS`+`FAIL`, `skipped` — число `SKIP`.
4. Если структурные совпадения были, но все дали `SKIP`, исход всего правила —
   `SKIP` независимо от `onEmpty`.
5. Иначе агрегат вычисляется над эффективной популяцией.

Таким образом, `onEmpty` означает именно отсутствие структурных совпадений, а не
отсутствие вычислимых результатов. Обязательность второго операнда выражается
отдельным presence-правилом, а не `onEmpty: "FAIL"`.

**Режимы.**

| mode | PASS когда |
| --- | --- |
| `ALL` | каждый вычисленный элемент дал `PASS` |
| `ANY` | хотя бы один вычисленный элемент дал `PASS` |
| `COUNT` | число `PASS` удовлетворяет `op value` |
| `MIN` | оператор даёт `PASS` на минимальном структурно совпавшем значении |
| `MAX` | оператор даёт `PASS` на максимальном структурно совпавшем значении |

**`MIN`/`MAX` выбирают до вычисления оператора.** `[D22]` Для этих режимов шаги
эффективной популяции выше не применяются. После резолюции структурного списка:

1. Если список пуст, применяется `onEmpty`.
2. Все исходные значения классифицируются по §2.5. Если любое значение
   неклассифицируемо либо виды различаются, результат — `FAIL`, оператор не
   вызывается.
3. Выбирается минимальное или максимальное исходное значение; tie-break при
   равенстве берёт первый элемент в нормативном порядке §3.6.1.
4. Оператор вызывается ровно один раз — на выбранном элементе. Его
   `PASS`/`FAIL`/`SKIP` становится исходом всего агрегата.

Таким образом, невыбранные элементы не могут дать `SKIP`, `OPERATOR_FAULT` или
`OPERATOR_CONTRACT_VIOLATION`; `SKIP` выбранного экстремума распространяется на
весь результат без противоречия с правилами эффективной популяции.

**Порождение issues.** Issues возможны только при итоговом `FAIL` агрегата:

- `ALL + EACH` — issue на каждый элемент с исходом `FAIL`;
- `ANY + EACH` — если ни один элемент не дал `PASS`, issue на каждый `FAIL`;
- `ALL/ANY + SUMMARY` — один summary-issue;
- `COUNT`, `MIN`, `MAX` и `onEmpty: "FAIL"` — один summary-issue.

Элементы `SKIP` никогда не порождают issues. Поэтому успешный `ANY` не создаёт
частичных issues из-за остальных `FAIL`.

## 4. Форматы артефактов

### 4.1 Общие правила артефактов

Артефакт — JSON-объект. Каждый артефакт любого типа MUST иметь:

| Поле | Тип | Ограничение |
| --- | --- | --- |
| `id` | строка | непустая; уникальна в пределах снэпшота |
| `type` | строка | одно из `"rule"`, `"condition"`, `"pipeline"`, `"dictionary"` |

Снэпшот с артефактом, нарушающим эти правила, или с двумя артефактами с одним `id`
— **невалиден** (§4.10). Неизвестные поля верхнего уровня артефакта делают его
невалидным (закрытые схемы: опечатки громкие, неймспейс зарезервирован под будущие
версии спецификации). Поле `description` не является частью исполняемого формата:
названия, пояснения, папки, теги и владельцы принадлежат authoring-слою или
`snapshot.meta` и не влияют на `sourceHash`. `[DR-II][D21]`

### 4.2 Идентификаторы

`id` — непрозрачная непустая строка, сравниваемая точным регистрозависимым
равенством Unicode-последовательности. Точки, косые черты и иные символы не создают
иерархии и не влияют на адресацию. Зарезервированных префиксов нет: `library.*`,
`internal.*` и `entrypoints.*`, если используются authoring-инструментом, являются
обычными частями id без нормативной семантики. `[D21]`

Все id глобально уникальны внутри снэпшота. Владение артефактом, папки, области
видимости, локальные имена, сиротские скоупы и выбор владельца по префиксу в
исполняемой модели отсутствуют.

### 4.3 Резолюция ссылок

Любая строка-ссылка `r` в шаге, `when`-выражении или ссылке на словарь означает
ровно артефакт с `id === r`. Не существует относительного раскрытия, импортного
алиаса или зависимости от места использования. Ссылка, не находящая артефакт либо
находящая артефакт не того типа для своей позиции, делает снэпшот невалидным.

Импорты, алиасы и устранение конфликтов id выполняются сборщиком до создания
снэпшота. Нормативный снэпшот уже содержит замкнутый граф с точными ссылками.

### 4.4 Артефакт `rule`

Поля сверх §4.1:

| Поле | Обязательно | Ограничение |
| --- | --- | --- |
| `operator` | да | встроенный оператор (§3.2) либо оператор из `requires.operators` (§4.9) |
| `field` | по оператору | путь §2.7; не используется `any_filled` |
| `fields` | только `any_filled` | непустой массив путей |
| `value`, `value_field`, `flags`, `dictionary` | по оператору | раздел 3; `value_field` не содержит `[*]` |
| `params` | только пользовательский оператор | JSON-объект, валидный по закрытой схеме зарегистрированного оператора `[D24]` |
| `aggregate` | при `[*]` в `field` | §3.6; на поле без wildcard запрещён |
| `issue` | опционально | описание issue при `FAIL` в rule-шаге |

Объект `issue` закрыт и имеет форму:

```json
{
  "level": "WARNING | ERROR | EXCEPTION",
  "code": "непустая строка",
  "message": "непустая строка",
  "meta": { "optional": "любой JSON-объект" }
}
```

`level`, `code` и `message` обязательны вместе. `code` уникален среди всех правил
с `issue` в снэпшоте. `meta`, если есть, пробрасывается в созданные этим правилом
issues (§6.6) и не влияет на вычисление. Полей `role`, `level`, `code`, `message`
или `meta` верхнего уровня правила в fv2 нет. `[D19]`

Правило без `issue` является полноценным условием и MAY использоваться в `when`.
Rule-шаг MUST ссылаться на правило с `issue`, иначе снэпшот невалиден. Правило с
`issue` MAY одновременно использоваться и в шагах, и в `when`; на `when`-сайте
объект `issue` игнорируется.

**Параметры пользовательского оператора.** `params` запрещён для встроенных
операторов. Зарегистрированный пользовательский оператор определяет, какие из
стандартных операндных полей (`field`, `fields`, `value`, `value_field`, `flags`,
`dictionary`) он принимает, и регистрирует закрытую compile-time-схему `params`.
Несоответствие этой схеме отвергает снэпшот до исполнения. Конкретный API и язык
описания схемы реализация выбирает сама; пакет оператора, заявляющий переносимость
между рантаймами, MUST публиковать одну эквивалентную машиночитаемую схему для всех
реализаций. Ядро передаёт принятый `params` оператору дословно.

Строки внутри `params` не являются ссылками на артефакты и не создают рёбер графа
снэпшота. Зависимость от словаря MUST выражаться нормативным полем `dictionary`, а
зависимости от payload/context, например дополнительные пути полей, проверяются
схемой самого оператора.

### 4.5 Артефакт `condition`

| Поле | Обязательно | Ограничение |
| --- | --- | --- |
| `when` | да | when-выражение |
| `steps` | да | непустой массив шагов (§4.8) |

When-выражение — одно из:

```text
"rule_ref"
{ "all": [ <when-выражение>, … ] }
{ "any": [ <when-выражение>, … ] }
{ "not": <when-выражение> }
```

Объектная форма закрыта и содержит ровно один ключ `all`, `any` или `not`; иных
полей нет. Массивы `all`/`any` MUST быть непустыми. Каждый лист обязан ссылаться на любой
валидный артефакт `rule`; наличие и уровень `rule.issue` значения не имеют.
Ссылка на условие, pipeline или dictionary делает снэпшот невалидным. Семантика —
в §5.4. `[D19]`

### 4.6 Артефакт `pipeline`

| Поле | Обязательно | Ограничение |
| --- | --- | --- |
| `strict` | да | boolean, явно |
| `flow` | да | непустой массив шагов (§4.8) |
| `message` | при `strict: true` | непустая строка; сообщение strict-summary-issue |
| `strictCode` | опционально, только при `strict: true` | непустая строка; дефолт `"STRICT_PIPELINE_FAILED"` |

Публичные точки входа перечисляются централизованно в `exports` (§4.9);
поля `entrypoint` у артефакта нет. Семантика `strict` — в §5.7. `message` или
`strictCode` у нестрогого пайплайна невалидны.

**Без поля требований к контексту.** `[D14]` Пайплайны не декларируют
`required_context` (неизвестное поле при закрытых схемах). Полнота runtime-контекста
— явное решение аналитика, выраженное обычными правилами на путях `$context.*` —
например, `not_empty`-правилом `$context.currentDate` с объектом `issue`, который
задаёт аналитик, — размещённым там, где аналитик хочет
гарантию (обычно первым шагом `flow` точки входа). Внимание на взаимодействие со
skip-семантикой `[D13]`: value-проверки с отсутствующим операндом `$context.*`
скипаются, поэтому сценарий, зависящий от контекста, MUST гардить его явно, если
отсутствие должно быть ошибкой.

**DAG управления потоком.** `[D21]` Узлы графа — все pipeline- и
condition-артефакты. Ребро существует для каждого шага `{ "pipeline": … }` или
`{ "condition": … }` внутри `pipeline.flow` либо `condition.steps`. Весь этот
объединённый граф MUST быть ацикличен. Поэтому запрещены как condition → condition,
так и смешанные циклы pipeline → condition → pipeline. Ссылки `when` на правила и
ссылки правил на словари в управляющий DAG не входят.

### 4.7 Артефакт `dictionary`

| Поле | Обязательно | Ограничение |
| --- | --- | --- |
| `entries` | да | непустой массив |

Каждая запись — скаляр (строка, число, boolean; `null` невалиден) или объект хотя
бы с одним из полей `code`, `value`, каждое из которых само MUST быть скаляром
(строкой, числом или boolean) `[DR-IV]`. Объект записи закрыт: иных полей нет.
Семантика матчинга — §3.5.

### 4.8 Шаги

Шаг — закрытый объект ровно с одним из ключей `rule`, `condition`, `pipeline`, плюс
опциональный `stepId` (непустая строка; пробрасывается в issues и trace).

| Ключ | Обязан ссылаться на | Резолюция |
| --- | --- | --- |
| `rule` | артефакт `rule` с объектом `issue` | точный id §4.3 |
| `condition` | артефакт `condition` | точный id §4.3 |
| `pipeline` | артефакт `pipeline` | точный id §4.3 |

Rule-шаг на правило без `issue` делает снэпшот невалидным: при `FAIL` рантайм не
имел бы нормативных данных для issue. `[D19][DR-II]`

### 4.9 Формат снэпшота (`formatVersion: 2`)

Снэпшот — единица дистрибуции и единица конформанса. `[D11]`

```json
{
  "format": "jsonspecs-snapshot",
  "formatVersion": 2,
  "specVersion": "1.0.0-rc.4",
  "sourceHash": "<64 hex-символа в нижнем регистре>",
  "requires": { "operators": ["valid_inn"] },
  "exports": ["credit.application"],
  "artifacts": [ … ],
  "meta": {
    "projectId": "…",
    "projectTitle": "…",
    "description": "…",
    "rulesetVersion": "…"
  }
}
```

| Поле | Обязательно | Ограничение |
| --- | --- | --- |
| `format` | да | ровно `"jsonspecs-snapshot"` |
| `formatVersion` | да | ровно `2` (целое) |
| `specVersion` | да | полная SemVer-2.0.0-версия спецификации, на которую нацелен снэпшот |
| `sourceHash` | да | SHA-256, hex в нижнем регистре, от нормативной проекции ниже по RFC 8785 (JCS) `[D6][D25]` |
| `requires.operators` | опционально | непустой массив уникальных имён не-встроенных операторов, используемых артефактами `[D10]` |
| `exports` | да | непустой массив уникальных точных id pipeline-артефактов; публичный API бандла и корни замыкания `[D21]` |
| `artifacts` | да | непустой массив артефактов; порядок массива сам по себе незначим |
| `meta` | опционально | свободные метаданные проекта; `projectId` и `rulesetVersion`, если присутствуют, переносятся в `ruleset` результата (раздел 6) |

Конверт снэпшота закрыт: кроме перечисленных полей верхнеуровневых полей нет.
`requires` — закрытый объект ровно с `operators`; `exports` — непосредственно массив
id пайплайнов, иных видов экспорта нет. `meta` — единственное намеренно открытое расширение, MUST быть
JSON-объектом и не участвует в принятии, вычислении или `sourceHash`.

**Нормативная проекция `sourceHash`.** `[D25]` Перед JCS-хешированием строится:

```json
{
  "requires": { "operators": ["<имена по возрастанию>"] },
  "exports": ["<id по возрастанию>"],
  "artifacts": ["<артефакты по id по возрастанию>"]
}
```

Если `requires` отсутствует, в проекции используется пустой `operators`. Сортировка
строк и id — лексикографическая по кодовым точкам. Содержимое каждого артефакта не
переупорядочивается сверх обычной JCS-сортировки ключей: порядок `flow`, `steps`,
`when`-массивов и dictionary entries сохраняется. Поэтому один граф, собранный из
БД в разном порядке строк, имеет один `sourceHash`, а изменение публичных exports
или требований операторов меняет хэш.

**Принятие по версии.** Реализация декларирует точный диапазон поддерживаемых
значений `specVersion` и MUST отвергать любой снэпшот со `specVersion` вне этой
декларации — всё более слабое позволяет двум реализациям принимать разные множества
снэпшотов, что разрушает саму цель. Prerelease-идентификаторы сравниваются по
прецедентности SemVer. `[DR-IV]`

Снэпшот пиннит контракт поведения (`specVersion`), никогда — версию реализации.
Реализации MAY читать implementation-specific-подсказки из `meta`, но MUST NOT
основывать на них принятие. `[DR-II]`

**Замыкание операторов.** Каждый `operator`, названный правилом, MUST быть либо
встроенным (раздел 3), либо перечисленным в `requires.operators`. Правило с
оператором, не являющимся ни тем, ни другим, невалидно. Реализация, не
предоставляющая какой-либо оператор из `requires.operators`, MUST отвергнуть
снэпшот до любого исполнения — идентификатор `OPERATOR_NOT_FOUND` (раздел 6).
`[D10]`

**Полное замыкание артефактов.** `[D21]` Строится граф достижимости от всех
`exports`. Рёбра ведут из pipeline/condition по всем их шагам; из
condition — также ко всем rule-листьям `when`; из rule — к словарю в нормативном
поле `dictionary`. Множество достижимых id MUST в точности совпадать с множеством
всех `artifacts`: недостижимый rule, condition, pipeline или dictionary делает
снэпшот невалидным. Исходный проект MAY хранить неиспользуемые файлы, но готовый
production-снэпшот их не содержит.

### 4.10 Множество отвержения

Конформная реализация MUST отвергнуть — отказаться исполнять любой пайплайн —
снэпшот с любым из следующего. *Когда* происходит отвержение (загрузка, подготовка,
первый вызов) — ненаблюдаемо и не ограничивается `[D12]`; *что* оно происходит до
любого исполнения — нормативно.

1. Нарушения закрытого конверта снэпшота или вложенных схем `requires`/`exports`:
   неверные `format`/`formatVersion`, некорректный или неподдерживаемый
   `specVersion`, отсутствующий/некорректный `sourceHash`, пустые/дублирующиеся или
   неверно типизированные exports, либо хэш, не совпадающий с §4.9.
2. Любой артефакт, нарушающий §4.1 (включая дубликат `id`) или свою типовую схему
   (§4.4–§4.8), включая regex-паттерны вне грамматики или лимитов §3.4, невалидные
   `flags`, незакрытые вложенные объекты, невалидные комбинации `aggregate`,
   `issueMode` и `onEmpty`, либо `params`, не принятый схемой оператора.
3. Дубликат `issue.code` среди правил с `issue`.
4. Любая неразрешённая или неправильно типизированная точная ссылка (§4.3), включая
   when-листья не на правила и rule-шаги на правила без `issue`.
5. Цикл в объединённом управляющем DAG pipeline/condition (§4.6).
6. Имя оператора, не являющееся ни встроенным, ни задекларированным в
   `requires.operators`; либо задекларированный обязательный оператор, который
   реализация не предоставляет (`OPERATOR_NOT_FOUND`).
7. Артефакт, `params` или значение `meta`, превышающее максимальную глубину JSON
   (§2.1), либо числовой токен любой части снэпшота, не преобразующийся в конечный
   binary64 (§2.2).
8. Артефакт, недостижимый из `exports` по полному замыканию §4.9.

Две конформные реализации MUST совпадать по членству в этом множестве для каждого
входа. *Тексты* и *гранулярность* диагностик отвержения — информативны; вердикт —
нормативен. `[D7]`

---

## 5. Семантика исполнения

### 5.1 Вход исполнения и валидация входа

Исполнение — чистая функция кортежа:

```
(snapshot, pipelineId, payload, context?)
```

Валидация входа идёт в следующем нормативном порядке; первый провал определяет
исход (результат `ABORT`, раздел 6), поэтому реализации совпадают в том, *какая*
ошибка репортится, когда применимо несколько:

1. **Выбор пайплайна.** `pipelineId` MUST быть непустой строкой — иначе `ABORT`
   `INVALID_PIPELINE_ID`, `details: {"expected":"non-empty string"}`. Строка MUST
   точно совпадать с одним id из `exports`; неизвестный или внутренний,
   но не экспортированный id даёт `PIPELINE_NOT_FOUND`,
   `details: {"pipelineId":"<заданный>"}`. Перечисление доступных exports — задача
   диагностического инструментария, а не runtime-ошибки. `[D7][D21]`
2. **Типы контейнеров.** `payload` MUST быть JSON-объектом — иначе `ABORT` с
   `INVALID_PAYLOAD`, `details: {"expected": "object"}` (покрывает `null`, массивы,
   скаляры). `context`, если присутствует, MUST быть JSON-объектом — иначе
   `INVALID_CONTEXT`, те же details. Payload проверяется раньше context.
3. **Скан ключей.** `[D15]` Ключ *опасен*, если он — зарезервированный ключ (§2.1);
   ключ *недопустим*, если он пуст (`""`) или содержит `.`, `[` или `]`. Скан идёт
   top-down и не входит в поддерево под опасным или недопустимым ключом — нарушение
   *видимо* только когда все ключи-предки чисты, поэтому его `parentPath` — всегда
   корректный dot-path (корень = `""`). Среди видимых нарушений приоритет: опасные в
   payload, опасные в context, недопустимые в payload, недопустимые в context;
   внутри класса — лексикографически наименьшая пара `(parentPath, key)` (порядок
   кодовых точек, `parentPath` первым). Коды: `DANGEROUS_PAYLOAD_KEY`,
   `DANGEROUS_CONTEXT_KEY`, `INVALID_PAYLOAD_KEY`, `INVALID_CONTEXT_KEY`; details:
   `{"parentPath": "…", "key": "…"}`.
4. **Числа.** Все числовые значения рекурсивно приводятся к binary64 по §2.2.
   Переполнение в payload даёт `INVALID_PAYLOAD_NUMBER`, в context —
   `INVALID_CONTEXT_NUMBER`; details: `{"path":"…"}`. Payload проверяется раньше
   context; внутри документа выбирается лексикографически минимальный путь по
   кодовым точкам. Значение `-0` нормализуется в `0`.
5. **Глубина.** Глубина §2.1 payload ≤ 256 — иначе `PAYLOAD_TOO_DEEP`,
   `details: {"maxDepth": 256}`; аналогично context — `CONTEXT_TOO_DEEP`.

Все провалы валидации входа — `ABORT` (исполнение не могло быть выполнено).
Отдельной фазы required-context не существует `[D14]`: полнота контекста
проверяется обычными правилами, внутри flow, как всё остальное. Флаттенинг payload
(§2.7) — не шаг валидации: любой JSON-объект флаттенится.

### 5.2 Порядок исполнения

`flow` выбранного пайплайна исполняется последовательно. `condition`-шаг с
истинным гардом исполняет свои `steps` последовательно на месте; `pipeline`-шаг
исполняет `flow` пайплайна по ссылке на месте. Результирующий порядок вычислений
правил — depth-first, слева направо по дереву шагов — *документный порядок*.
Issues появляются в результате в документном порядке; внутри одного вычисления
правила поэлементные issues следуют порядку перечисления wildcard (§3.6.1);
strict-summary — по §5.7. `[D5]`

Реализации MUST NOT переупорядочивать, параллелить или дедуплицировать наблюдаемые
эффекты любым способом, меняющим нормативный результат. (Внутренний параллелизм
допустим при неотличимом результате.)

### 5.3 Rule-шаги

Rule-шаг вычисляет правило по разделу 3. `PASS` и `SKIP` не создают нормативных
эффектов. `FAIL` создаёт issue из `rule.issue` и данных исполнения: конкретного
`field`, `ruleId`, непосредственно объемлющего `pipelineId`, опционального
`stepId`, `expected`/`actual` и групповых `details` по разделу 6. `[D19]`

Уровень созданного issue определяет продолжение потока по §5.6. Оператор не может
изменить уровень, код или сообщение правила.

### 5.4 Condition-шаги

Лист `when` вычисляет правило по разделу 3 и преобразует исход: `PASS` → `true`,
`FAIL` и `SKIP` → `false`. Объектные выражения вычисляются нормативно
последовательно слева направо с short-circuit `[D22]`:

- `all` останавливается на первом `false`; если его нет, возвращает `true`;
- `any` останавливается на первом `true`; если его нет, возвращает `false`;
- `not` вычисляет единственного потомка и инвертирует его boolean.

Вычисление `when` никогда не создаёт issues и полностью игнорирует `rule.issue`,
включая `level: "EXCEPTION"`. Невычисленные после short-circuit листья не вызывают
оператор и не могут породить `ABORT` или trace. Брошенное исключение либо нарушение
контракта в фактически вычисленном листе даёт сайт-независимый `ABORT` (§3.1,
§6.7). Поэтому `any(PASS, throw) → true`, `all(FAIL, throw) → false`, а
`any(FAIL, throw)` и `all(PASS, throw)` дают `OPERATOR_FAULT`.

Если guard равен `true`, `steps` исполняются на месте; иначе шаг ничего не делает.

### 5.5 Pipeline-шаги

Pipeline-шаг исполняет `flow` пайплайна по ссылке на месте. Issues, порождённые
внутри, несут id *внутреннего* пайплайна как `pipelineId`. `strict` внутреннего
пайплайна применяется к его собственному поддереву исполнения (§5.7). Наличие
внутреннего пайплайна в `exports` на вызов шагом не влияет.

### 5.6 Уровни

| `level` | Issue | Эффект на поток | Вклад в `status` |
| --- | --- | --- | --- |
| `WARNING` | создаётся | нет | `OK_WITH_WARNINGS`, если нет сильнее |
| `ERROR` | создаётся | нет — исполнение продолжается | `ERROR` |
| `EXCEPTION` | создаётся | **всё исполнение немедленно останавливается** — включая оставшиеся шаги всех внешних пайплайнов | `EXCEPTION` |

Накопленные issues всегда сохраняются. Матрица `status`/`control` и статус `ABORT`
— в разделе 6.

### 5.7 Строгие пайплайны

Для пайплайна со `strict: true`, когда его поддерево исполнения завершается — хоть
прохождением до конца, хоть остановкой по `EXCEPTION` внутри — и внутри этого
поддерева (его собственные правила, условия и суб-пайплайны включительно) был
порождён хотя бы один issue уровня `ERROR` или `EXCEPTION`, после всех issues
поддерева добавляется один summary-issue:

```json
{
  "kind": "ISSUE",
  "level": "EXCEPTION",
  "code": "<strictCode или STRICT_PIPELINE_FAILED>",
  "message": "<pipeline.message>",
  "field": null,
  "ruleId": "pipeline:<pipelineId>",
  "pipelineId": "<pipelineId>"
}
```

и всё исполнение останавливается (как при любом `EXCEPTION`). Отдельные правила
внутри строгого пайплайна сохраняют декларированные уровни — эскалирует только
групповой исход. Вложенные строгие пайплайны применяют правило каждый к своему
поддереву; внутренний сработавший добавляет summary первым, остановка
распространяется наружу (внешние строгие, тоже сработавшие, добавляют свои summary
в порядке изнутри-наружу). `[DR-II]`

Коды strict-summary генерируются рантаймом и потому вне правила уникальности
`issue.code` правил (§4.4); авторам тем не менее SHOULD избегать коллизий значений
`strictCode` с кодами правил — потребители различают issues по `code`.

---

## 6. Контракт результата

### 6.1 Конверт результата и JSON-представление

Результат исполнения — JSON-объект:

```json
{
  "status": "OK | OK_WITH_WARNINGS | ERROR | EXCEPTION | ABORT",
  "control": "CONTINUE | STOP",
  "issues": [ … ],
  "ruleset": { … },
  "error": { … },
  "trace": [ … ]
}
```

`error` присутствует ровно тогда, когда `status` — `ABORT` (и `issues` тогда пуст).
`trace` опционален и целиком информативен (§6.9).

**Правила представления.** `[D8]` Результат — чистый JSON. «Нет значения»
выражается **опусканием ключа**, никогда — `null`, с единственным сознательным
исключением `"field": null` у issues, не относимых к одному полю (§6.3).
Нормативное равенство результатов — **структурное равенство JSON**: порядок
элементов массивов значим; порядок ключей объектов — нет; числа сравниваются как
binary64-значения (`1` равно `1.0`); детали сериализации (пробелы, стиль
экранирования, порядок ключей на проводе) — не часть конформанса.

### 6.2 Статус и control

| `status` | Смысл | `control` |
| --- | --- | --- |
| `OK` | нет issues | `CONTINUE` |
| `OK_WITH_WARNINGS` | только `WARNING`-issues | `CONTINUE` |
| `ERROR` | хотя бы один `ERROR`-issue, без `EXCEPTION` | `STOP` |
| `EXCEPTION` | хотя бы один `EXCEPTION`-issue (§5.6, §5.7) | `STOP` |
| `ABORT` | исполнение не могло быть выполнено (§6.7) | `STOP` |

`status` полностью определяется сильнейшим присутствующим уровнем issue (или
абортом); `control` полностью определяется `status`. Оба поля существуют потому,
что потребители часто ветвятся по биту go/no-go, не интерпретируя статусы.

### 6.3 Issues

Каждый issue — объект со следующими полями; колонка «Когда присутствует»
нормативна — поле, которое колонка исключает, MUST быть опущено:

| Поле | Тип | Когда присутствует |
| --- | --- | --- |
| `kind` | `"ISSUE"` | всегда |
| `level` | `WARNING \| ERROR \| EXCEPTION` | всегда |
| `code` | строка | всегда — авторское (`rule.code`, `strictCode`) |
| `message` | строка | всегда — авторское (`rule.message`, `pipeline.message`); нормативно как проброс данных `[D7]` |
| `field` | строка или `null` | всегда; конкретный разрезолвленный путь для полевых issues (для wildcard-элементов — конкретный сматченный путь вроде `x[2].v`); паттерн (`x[*].v`) для групповых summary; `null` для issues, не относимых к одному полю (`any_filled`, strict-summary) |
| `ruleId` | строка | всегда; `id` правила либо `pipeline:<pipelineId>` для strict-summary |
| `pipelineId` | строка | всегда; непосредственно объемлющий пайплайн (§5.5) |
| `stepId` | строка | только когда породивший шаг его декларирует |
| `expected` | значение | по §6.4 |
| `actual` | значение | по §6.4; опущено, когда единого actual-значения нет |
| `details` | объект | только групповые summary-issues (§6.5); нормативные машиночитаемые факты issue, зеркально `error.details` при `ABORT` (§6.7) — один паттерн на весь контракт: `code` идентифицирует класс, `details` несёт факты класса |
| `meta` | объект | когда `rule.issue` декларирует `meta` (§6.6) |

Порядок issues в `issues[]` нормативен: документный порядок вычислений правил
(§5.2), порядок перечисления wildcard внутри правила (§3.6.1), strict-summary по
§5.7.

### 6.4 `expected` и `actual`

| Класс операторов | `expected` | `actual` |
| --- | --- | --- |
| сравнения со значением (`equals`, `not_equals`, `contains`, `matches_regex`, `not_matches_regex`, `greater_than`, `less_than`, `length_*`) | `value` правила дословно | разрезолвленное значение поля |
| проверки типа (`is_boolean`, `is_string`, `is_number`, `is_integer`) | опущен | разрезолвленное значение поля |
| `not_empty` | опущен | опущен при отсутствии; значение (`null` или `""`) при присутствующем-но-пустом — как JSON-значение, т.е. `"actual": null` здесь легален как *значение* actual, в отличие от опускания ключа |
| `is_empty`, `not_true` | опущен | разрезолвленное значение поля |
| словарные операторы | объект `dictionary` правила дословно (`{"type":"static","id":…}`) | разрезолвленное значение поля |
| `field_*_field` | разрезолвленное **значение** `value_field` `[DR-III]` | разрезолвленное значение `field` |
| `any_filled` | опущен | опущен |

Поскольку флаттенинг допускает в листьях только скаляры и пустые контейнеры
(§2.7), значения `expected`/`actual` всегда мелкие; их обрезание или транспортная
нормализация не определяются и не допускаются.

### 6.5 Групповые summary-issues

Summary-issue создаётся при итоговом `FAIL` для `ALL/ANY + SUMMARY`, `COUNT`,
`MIN`/`MAX` либо `onEmpty: "FAIL"`. `[D20]`

| Источник | `details` | `expected` / `actual` |
| --- | --- | --- |
| `ALL`/`ANY`, `issueMode: "SUMMARY"` | `{"mode":"ALL|ANY","matched":<m>,"evaluated":<e>,"skipped":<s>,"passed":<p>,"failed":<f>}` | опущены / опущены |
| провал `COUNT` | `{"mode":"COUNT","op":"…","value":<v>,"matched":<m>,"evaluated":<e>,"skipped":<s>,"passed":<p>,"failed":<f>}` | опущены / опущены |
| провал `MIN`/`MAX` | `{"mode":"MIN|MAX"}` | по §6.4; `field` — конкретный путь экстремума, `actual` — его значение |
| `onEmpty: "FAIL"` | `{"mode":"<mode>","matched":0,"evaluated":0,"skipped":0,"passed":0,"failed":0}` | опущены / опущены |

Здесь `matched` — число структурных wildcard-совпадений, `evaluated` — число
результатов `PASS`+`FAIL`, `skipped` — число `SKIP`, `passed` и `failed` —
соответствующие части эффективной популяции. Всегда выполняются
`matched = evaluated + skipped` и `evaluated = passed + failed`.

Если экстремум `MIN`/`MAX` неопределён из-за несравнимых значений, `field` равен
wildcard-паттерну, а `expected`/`actual` опущены. Поэлементные issues режима `EACH`
являются обычными issues §6.3–§6.4 и не несут `details`.

### 6.6 Проброс `meta`

Когда `rule.issue` содержит `meta`, каждый issue этого правила несёт `meta`
дословно. Рантайм MUST NOT изменять его; факты исполнения живут в `details` или
нигде. На `when`-сайте весь объект `issue`, включая `meta`, игнорируется. `[DR-III][D19]`

### 6.7 `ABORT` и два канала отказа

Каналов отказа два; их смешение — ошибка конформанса:

**Канал A — отвержение снэпшота (§4.10).** Снэпшот отвергается до любого
исполнения. *Вердикт* нормативен; форма репортинга (список диагностик, брошенная
ошибка, exit-код) — на усмотрение реализации. Одна причина отвержения имеет
нормативный идентификатор, потому что зависит от окружения: `OPERATOR_NOT_FOUND` —
`requires.operators` снэпшота именует оператор, который реализация не
предоставляет. `[D10]`

**Канал B — `ABORT` исполнения.** Кортеж был принят к исполнению, но исполнение не
могло быть выполнено. Результат несёт:

```json
"error": { "code": "…", "message": "…", "details": { … } }
```

`code` и `details` нормативны; `message` — информативный свободный текст. `[D7]`
Нормативный enum кодов и точная форма `details` по кодам:

| `code` | Когда | `details` |
| --- | --- | --- |
| `INVALID_PIPELINE_ID` | §5.1 шаг 1 | `{"expected":"non-empty string"}` |
| `PIPELINE_NOT_FOUND` | §5.1 шаг 1 | `{"pipelineId": "<запрошенный>"}` |
| `INVALID_PAYLOAD` | §5.1 шаг 2 | `{"expected": "object"}` `[DR-IV]` |
| `INVALID_CONTEXT` | §5.1 шаг 2 | `{"expected": "object"}` `[DR-IV]` |
| `DANGEROUS_PAYLOAD_KEY` / `DANGEROUS_CONTEXT_KEY` | §5.1 шаг 3 | `{"parentPath": "…", "key": "…"}` — наименьшая видимая пара `(parentPath, key)` по §5.1; детерминизм при любом порядке обхода `[DR-III][D15]` |
| `INVALID_PAYLOAD_KEY` / `INVALID_CONTEXT_KEY` | §5.1 шаг 3 | `{"parentPath": "…", "key": "…"}` — то же правило выбора `[D15]` |
| `INVALID_PAYLOAD_NUMBER` / `INVALID_CONTEXT_NUMBER` | §5.1 шаг 4 | `{"path":"…"}` — лексикографически минимальный путь переполнения `[D23]` |
| `PAYLOAD_TOO_DEEP` / `CONTEXT_TOO_DEEP` | §5.1 шаг 5 | `{"maxDepth": 256}` — и ничего больше: *какой* путь первым превысит лимит, зависит от порядка обхода, поэтому путь не появляется ни в одной нормативной поверхности (информативный `message` MAY его нести) `[DR-III]` |
| `OPERATOR_FAULT` | реализация оператора бросила исключение/упала при исполнении | `{"ruleId": "…", "operator": "…"}` |
| `OPERATOR_CONTRACT_VIOLATION` | оператор вернул значение вне своей декларированной формы результата (§3.1) | `{"ruleId": "…", "operator": "…"}` |

Enum закрыт для этой версии спецификации: реализации MUST NOT эмитить иные коды в
канале B. Встроенные операторы никогда не порождают два кода `OPERATOR_*`; они
переносимо тестируются через зарезервированные conformance-операторы §7.3.

### 6.8 Провенанс `ruleset`

```json
"ruleset": {
  "specVersion": "1.0.0-rc.4",
  "sourceHash": "…",
  "projectId": "…",
  "rulesetVersion": "…",
  "engineVersion": "…"
}
```

| Поле | Присутствие | Поверхность |
| --- | --- | --- |
| `specVersion` | всегда — эхо снэпшота | нормативно `[DR-III]` — потребитель логирует, по какому контракту поведения получен результат, не держа снэпшот |
| `sourceHash` | всегда — эхо снэпшота | нормативно |
| `projectId`, `rulesetVersion` | когда присутствуют в `snapshot.meta` | нормативный проброс |
| `engineVersion` | опционально | **информативно** — идентификатор реализации, исключён из сравнения конформанса `[D7]` |

### 6.9 Trace

`trace` целиком информативен. `[D7]` Реализация MAY его опускать, порождать в
любой гранулярности или делать опциональным per-call. Когда порождается, ему
SHOULD быть массивом объектов-событий как минимум с `type` и идентификаторами
артефактов, и SHOULD фиксировать value-семантичные скипы (`[D13]`) и исходы гардов
условий — но никакое сравнение конформанса его не читает, а таймстампы (`at`)
делают побайтовое сравнение бессмысленным by design. Trace MUST NOT быть
единственным носителем какого-либо нормативного факта.

---

## 7. Конформанс

### 7.1 Декларация конформанса

Декларация конформанса называет: реализацию и версию; поддерживаемый диапазон
`specVersion` (§4.9); множество имён не-встроенных операторов, предоставляемых
инсталляцией. Межреализационное равенство нормативного результата гарантируется
**только для снэпшотов исключительно на встроенных операторах**. `[D17]` Равные
*имена* операторов не означают равную *семантику*: для снэпшотов с кастомными
операторами общим между реализациями является лишь контракт расширения (контракт
исхода §3.1, реакции `OPERATOR_*` §6.7 — оба переносимо тестируются
conformance-операторами §7.3) и параметризация множества отвержения (§4.10) через
`requires.operators` — один и тот же снэпшот легитимно принят инсталляцией,
предоставляющей `valid_inn`, и отвергнут (`OPERATOR_NOT_FOUND`) той, что не
предоставляет. Бизнес-поведение кастомного оператора — обещание его пакета, а не
этой спецификации. Для снэпшотов только на встроенных операторах конформанс
безусловен. `[D10]`

### 7.2 Нормативная проекция

Конформанс сравнивает *нормативную проекцию* поведения:

1. вердикт по снэпшоту: принят или отвергнут (§4.10), плюс идентификатор
   `OPERATOR_NOT_FOUND`, где применим;
2. для принятых снэпшотов и каждого кортежа исполнения: `status`, `control`,
   `issues[]` целиком (каждое поле §6.3, в порядке), `error.code` +
   `error.details` и `ruleset` без `engineVersion`.

Исключено из сравнения: `trace`, `engineVersion`, `error.message`, тексты и
гранулярность диагностик отвержения и всё, что реализация эмитит вне объекта
результата. Сравнение — структурное (§6.1).

### 7.3 Conformance-фикстуры

Дерево `fixtures/` репозитория `jsonspecs/spec` — **нормативное приложение**:
текст и фикстуры версионируются вместе, атомарно, одним тегом. Два вида фикстур:

```json
// evaluation-фикстура
{
  "name": "d02/…",
  "snapshot": { … },
  "operators": ["…"],            // зарегистрированное множество для фикстуры, дефолт []
  "input": { "pipelineId": "…", "payload": { … }, "context": { … } },
  "expected": {
    "status": "…", "control": "…",
    "issues": [ … ],
    "ruleset": { "specVersion": "…", "sourceHash": "…" }
  }
}

// rejection-фикстура
{
  "name": "d04/…", "snapshot": { … }, "operators": [],
  "expected": { "verdict": "reject", "identifier": "OPERATOR_NOT_FOUND"? }
}
```

Прогонщик исполняет каждую фикстуру против реализации и структурно сравнивает
нормативную проекцию. Прохождение полного набора версии X — **необходимое условие**
декларации конформанса для X, но не достаточное: набор сэмплирует пространство
поведения, текст его определяет. `[DR-IV]` При противоречии фикстуры тексту прав
текст; фикстура исправляется через erratum и новую версию набора — фикстуры никогда
молча не переопределяют текст. Фикстуры организованы по решениям и семантическим
областям; каждое решение D1–D25 покрыто как минимум одной фикстурой.

**Зарезервированные conformance-операторы.** `[DR-IV]` Следующие имена операторов
зарезервированы. Их регистрирует **только conformance-прогонщик** как часть
тестовой обвязки — никогда продакшен-рантаймы; каждая реализация адаптирует их к
своему API регистрации. Пиннёное поведение при вызове:

| Имя | Поведение | Ожидаемая реакция |
| --- | --- | --- |
| `conformance.rule.throw` | бросает хостовое исключение | `ABORT OPERATOR_FAULT` на любом сайте |
| `conformance.rule.invalid_result` | возвращает `EXCEPTION`, значение вне enum'а | `ABORT OPERATOR_CONTRACT_VIOLATION` на любом сайте |
| `conformance.rule.tri` | для значения `"PASS"`, `"SKIP"` или `"FAIL"` возвращает одноимённый исход | исход по §3.1; используется для смешанных агрегатных популяций |
| `conformance.rule.params` | не принимает стандартных операндов; требует закрытый объект params `{ "outcome": "PASS" | "FAIL" | "SKIP" }` и возвращает этот исход | фиксирует compile-time-валидацию схемы параметров и их передачу без изменений |

### 7.4 Сводка требований

Конформная реализация:

- MUST принимать и отвергать ровно множество §4.10, относительно своих
  зарегистрированных операторов;
- MUST выдавать идентичную нормативную проекцию для каждого кортежа исполнения;
- MUST проходить полный набор фикстур декларируемой версии спецификации;
- MUST NOT требовать какого-либо входа, флага или режима сверх кортежа исполнения
  для достижения перечисленного (конформанс — поведение по умолчанию, а не опция);
- MAY делать что угодно, не наблюдаемое через нормативную проекцию: компилировать
  или интерпретировать, кэшировать, параллелить, порождать любой trace,
  экспонировать любой API. `[D12]`

### 7.5 Вне скоупа

Граница в одном месте: API и сигнатуры функций; момент валидации; стратегия
компиляции и диагностика сверх вердикта; производительность и ресурсные лимиты
сверх нормативных (§2.1, §3.4.2); транспортное обрезание результатов; поведение
кастомных операторов `[D10]`; содержимое trace; legacy-поверхности прототипа
(`engine.minVersion`, `paths[]`, `payload.__context`, `required_context`)
`[D11][D14]`.
